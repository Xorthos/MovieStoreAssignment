
@{
    ViewBag.Title = "Index";
}

<h2>Genre</h2>
<table class="table table-striped table-condensed">
<thead>
    <tr>
        <th>Id</th>
        <th>Genre</th>
        <th></th>
    </tr>
    <</thead>>    
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@*@Html.ActionLink("Edit", "Edit", new { genreId== @item.Id})
                    @Html.ActionLink("Delete", "Delete", new {genreId == @item.Id })*@</td>

            </tr>

        }

    </tbody>
</table>
@Html.ActionLink("Create new Genre", "Create", null , new {@class= "btn btn-default preview-add-button" })

